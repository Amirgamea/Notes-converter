services:
  - type: web
    name: bear-converter
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: HOME
        value: /tmp
    healthCheckPath: /health